# /forge-contextmap — Graph-Powered Context Scaffold

This command has moved to a skill: `.claude/skills/forge-contextmap/`.

Invoke the **forge-contextmap** skill: read `.claude/skills/forge-contextmap/SKILL.md` (project
copy; fall back to `~/.claude/skills/forge-contextmap/SKILL.md`) and follow it exactly — its
STEP 1 routes to the right mode. Pass `$ARGUMENTS` through unchanged (`sync`, `--new`).
